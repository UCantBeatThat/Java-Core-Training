package com.training.services;

import com.training.models.*;

public class ItemService {
	
	public Rent getReference(int key) {
		switch(key) {
			case 1:
				return new Computers();
			case 2:
				return new Furniture();
			default:
				return null;
		}
	}
	
	public double getItemKeyRent(Rent item, int qty) {
		
		double itemRent = 0.0;
		
		if(item instanceof Computers) {
			Computers item1 = (Computers)item;
			itemRent = item1.calculateRent(qty);
		}
		else if(item instanceof Furniture) {
			Furniture item1 = (Furniture)item;
			itemRent = item1.calculateRent(qty);
		}
		
		return itemRent;
	}
}
