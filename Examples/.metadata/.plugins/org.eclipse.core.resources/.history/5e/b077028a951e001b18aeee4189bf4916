package com.training.utils;

public class Counter implements Runnable {

	@Override
	public void run() {
		
		Thread.currentThread().setName("Child-Thread");
		System.out.println("Thread in chld:" + Thread.currentThread());

	}

}
